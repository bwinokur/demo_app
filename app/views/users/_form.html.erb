<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :provider_id %><br />
    <%= @providers = Provider.find(:all, :order => 'id')
        collection_select(:user, :provider_id, @providers, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :provider_password %><br />
    <%= f.text_field :provider_password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
